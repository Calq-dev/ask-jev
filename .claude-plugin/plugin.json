{
  "name": "jev-ask",
  "version": "0.3.0",
  "description": "Ask Jev a question about a file instead of reading it into context.",
  "license": "MIT",
  "userConfig": {
    "apiKey": {
      "type": "string",
      "title": "TypeSafe API key",
      "description": "TypeSafe API key. Leave unset to use TYPESAFE_API_KEY from the environment.",
      "sensitive": true
    },
    "model": {
      "type": "string",
      "title": "Jev model",
      "description": "TypeSafe Jev model name.",
      "default": "jev-latest"
    },
    "maxChars": {
      "type": "number",
      "title": "Maximum characters per file",
      "description": "A file longer than this is cut; the tool says so in its answer.",
      "default": 60000
    }
  }
}
